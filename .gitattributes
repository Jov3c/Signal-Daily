# 统一换行：仓库内一律存 LF，与 Prettier 的 endOfLine: lf 保持一致。
* text=auto eol=lf

# 二进制文件不做换行转换。
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.zip binary
*.woff binary
*.woff2 binary

# lockfile 由工具维护，不计入语言统计。
pnpm-lock.yaml linguist-generated=true
